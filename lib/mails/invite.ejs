<%
  let verbString;

  if (verbs.length === 1) {
    verbString = verbs[0];
  } else if (verbs.length === 2) {
    verbString = `${verbs[0]} and ${verbs[1]}`;
  } else if (verbs.length > 2) {
    verbString = `${verbs.slice(0, -1).join(', ')}, and ${verbs.slice(0).pop()}`;
  } else {
    verbString = 'use';
  }
%>

Hello <%= name %>,

You have been invited to <%= verbString %> BNV Calculators. Please click the link below to activate your account:

<%= link %>

If the link wraps onto two or more lines, make sure to copy the entire link at once. Paste the copied link into the address bar of your browser. You will remain logged-in for 90 days or until you click "Sign out" in the top-right corner of the page.

--------------------------------------------------

This is an automated message intended for <%= email %>. Please do not reply.
